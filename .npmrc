# apps/docs deliberately consumes the PUBLISHED superlore (a real npm consumer), so plain
# semver ranges resolve from the registry. Workspace links use the explicit `workspace:` protocol.
link-workspace-packages=false
