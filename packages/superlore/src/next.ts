/**
 * `superlore/next` — the Next.js plugin that wires the content pipeline into your build.
 * Re-exported through superlore so `next.config.mjs` imports superlore, not the renderer.
 */
export { createMDX } from "fumadocs-mdx/next";
