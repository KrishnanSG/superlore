# starter

The `npm create superlore` starter — a ready-to-edit knowledge base that wires up the `superlore`
core, drops in example content, and is one command away from deploying to Vercel (or anywhere).

> Not yet implemented — Phase 3 in the [roadmap](../../docs/ROADMAP.md). Mirrors the structure
> of the source Fumadocs sites the core is extracted from.
