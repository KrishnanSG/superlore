---
title: Changelog
icon: Tag
description: What's new in superlore — every release as a complete record an agent can query, with a hero, highlights, grouped changes, and what's coming next.
summary: A product changelog rendered with Releases — a collision-free release timeline (shipped left of Now, upcoming to the right) plus tag filter, and each entry a full release record (hero media, a Highlights grid, changes grouped into sections) beside a sticky area rail, all serialized to a release knowledge node for the MCP.
tags: [changelog, releases, roadmap]
hideToc: true
---

Every superlore release is a complete record — a hero, the marquee highlights, and changes grouped
into sections — and every block is typed, so an agent over MCP reads the same release you do.

<Releases label="superlore changelog">
  <Release
    version="0.14.0"
    date="2026-06-28"
    status="done"
    title="The MCP serves the whole corpus — body, grep, and glob"
    tags={["MCP", "Agents"]}
    areas={["MCP", "Agents"]}
    summary="get_page now returns the page body (raw MDX when wired, else extracted prose) plus headings — not just frontmatter — and two new tools, grep and glob, let an agent find anything in the corpus the way it would grep a folder. Recommended for everyone — no breaking changes."
    highlights={[
      { icon: "file-text", title: "get_page returns content", body: "The readable body (raw MDX) + headings + typed nodes, so an agent can actually read a page — prose pages are no longer empty." },
      { icon: "search-code", title: "grep across the corpus", body: "Regex over every page body, line by line — ripgrep-style { path, line, text } hits, scoped by a path glob." },
      { icon: "folder-tree", title: "glob the page tree", body: "List page paths by shell-style pattern — the corpus's file finder." },
    ]}
    sections={[
      { label: "New", changes: [
        { type: "added", text: "MCP tool grep(pattern, flags?, path?, limit?) — regex search across page bodies, returning { path, line, text }.", refs: [{ target: "/docs/agents/mcp/tools", label: "MCP tools" }] },
        { type: "added", text: "MCP tool glob(pattern) — list page paths matching a shell-style glob." },
        { type: "added", text: "buildIndexFromSource(source, { readContent }) — wire a reader to serve the raw authored MDX body; without it, bodies fall back to extracted prose." },
      ]},
      { label: "Improved", changes: [
        { type: "changed", text: "get_page now returns the page body + headings, not just frontmatter — the index reads the real content, so search ranks over it too." },
        { type: "changed", text: "The scaffolded MCP route ships grep + glob and serves raw MDX out of the box." },
      ]},
      { label: "Breaking", changes: [] },
    ]}
  />

<Release
  version="0.13.0"
  date="2026-06-28"
  status="done"
  title="Releases, round two — multi-select filter, timeline options, hideToc"
  tags={["Releases", "Theming"]}
  areas={["Releases", "Theming"]}
  summary="The Releases component grows up: a multi-select feature filter, feature-tag chips back on the rail, a re-arranged + re-styled timeline with axis / strip variants, and a hideToc flag for self-navigating pages. Recommended for everyone — no breaking changes."
  highlights={[
    {
      icon: "list-filter",
      title: "Multi-select filter",
      body: "Filter the changelog by any number of features at once (OR). Renamed Filter features, with a count badge and one-click clear.",
    },
    {
      icon: "git-commit-horizontal",
      title: "Timeline, fixed + optioned",
      body: "Dots clear the month labels, crowded versions never overlap, the latest wears the accent — plus a new strip variant beside the date axis.",
    },
    {
      icon: "tags",
      title: "Tags back on the rail",
      body: "Each release shows its feature tags as chips again — and they double as filter toggles.",
    },
    {
      icon: "panel-right-close",
      title: "hideToc",
      body: 'Self-navigating pages (a Releases changelog) can drop the flat "On this page" list; the timeline and rail already navigate.',
    },
  ]}
  sections={[
    {
      label: "New",
      changes: [
        {
          type: "added",
          text: "Releases gains a timeline prop — axis (default, the date rail), strip (evenly-spaced stops), or off.",
        },
        {
          type: "added",
          text: "hideToc frontmatter flag hides the table of contents while keeping the centred layout — set it on a changelog.",
        },
      ],
    },
    {
      label: "Improved",
      changes: [
        {
          type: "changed",
          text: "The feature filter is multi-select and renamed Filter features; each release's rail tags double as filter toggles.",
        },
        {
          type: "changed",
          text: "Timeline layout: dots no longer collide with the month labels, stacked release cards never overlap, and the latest release is accented.",
        },
      ],
    },
    { label: "Breaking", changes: [] },
  ]}
/>

<Release
  version="0.12.0"
  date="2026-06-27"
  status="done"
  title="superlore 0.12 — themes, reimagined releases, runtime safety"
  tags={["Themes", "Releases", "Runtime"]}
  areas={["Themes", "Releases", "Runtime", "Discoverability", "Canvas", "MCP"]}
  summary="The first big release: a seven-theme system, a from-scratch release/changelog component, a render-time safety net, and an SEO + llms.txt discoverability layer. Recommended for everyone — no breaking changes."
  media={[
    {
      type: "video",
      provider: "file",
      src: "/releases/0.12.mp4",
      poster: "/releases/0.12.png",
      title: "superlore 0.12",
      caption: "The 0.12 release reel — themes, reimagined releases, runtime safety.",
    },
  ]}
  highlights={[
    {
      icon: "palette",
      title: "7-theme system",
      body: "default · mint · geist · ledger · obsidian · prism · paste — pick a skin with one flag in superlore.json, zero .mdx edits.",
    },
    {
      icon: "rocket",
      title: "Reimagined releases",
      body: "Media-first entries, a Highlights grid, grouped sections, and a collision-free timeline.",
    },
    {
      icon: "shield-check",
      title: "Runtime safety",
      body: "A bad token in prose degrades to a fallback instead of blanking the host page.",
    },
    {
      icon: "search",
      title: "Found by AI + search",
      body: "llms.txt + llms-full.txt, JSON-LD, and per-page canonical/OG — built in.",
    },
  ]}
  sections={[
    {
      label: "New features",
      changes: [
        {
          type: "added",
          text: "Theme system — seven production themes scoped to data-sl-theme; accent + font stay orthogonal brand overrides.",
          refs: [{ target: "/docs/themes", label: "Themes" }],
        },
        {
          type: "added",
          text: "Release component reimagined — media-first entries (image lightbox + video facade), a Highlights grid, grouped sections, and an area jump-rail.",
        },
        {
          type: "added",
          text: "superlore.json gains logo { light, dark, href } + favicon; accent now drives the whole palette.",
        },
        {
          type: "added",
          text: "Discoverability: /llms.txt + /llms-full.txt, JSON-LD, and per-page canonical + Open Graph.",
        },
        {
          type: "added",
          text: "A Why superlore page, plus a release-notes skill + Remotion reel pipeline.",
          refs: [{ target: "/docs/why-superlore", label: "Why superlore" }],
        },
      ],
    },
    {
      label: "Improvements",
      changes: [
        {
          type: "changed",
          text: "The release timeline never overlaps — a lane layout stacks crowded versions at different heights.",
        },
        {
          type: "changed",
          text: "Sidebar hierarchy — section headers read distinct from page items; collapse persists (⌘\\).",
        },
      ],
    },
    {
      label: "Fixed",
      changes: [
        {
          type: "fixed",
          text: "A bad {token} in prose now degrades gracefully instead of blanking the page (a render-time error boundary).",
        },
      ],
    },
  ]}
/>

<Release
  version="0.11"
  date="2026-06-23"
  status="done"
  title="Embeddable runtime + Powered-by badge"
  tags={["Runtime", "Branding"]}
  areas={["Runtime", "Branding", "MCP"]}
  summary="Drop a superlore doc into any host app — now with zero global CSS leakage and a floating brand badge."
  media={[
    {
      type: "image",
      src: "/dual-representation.png",
      alt: "superlore dual representation",
      caption: "One source — a clean page for humans, structured knowledge for agents.",
    },
  ]}
  highlights={[
    {
      icon: "shield-check",
      title: "Zero leakage",
      body: "Every rule is scoped to the doc container — import it app-wide and the host's own pages don't move a pixel.",
    },
    {
      icon: "badge-check",
      title: "Powered by superlore",
      body: "A small floating badge on every embedded doc — branding by default, one prop to remove.",
    },
  ]}
  sections={[
    {
      label: "New features",
      changes: [
        {
          type: "added",
          text: "superlore/runtime.css is fully scoped to a .superlore-doc container — import it app-wide with a 0-pixel host diff.",
        },
        {
          type: "added",
          text: "SuperloreDoc stamps a floating 'Powered by superlore' badge (opt out with badge=false).",
        },
      ],
    },
    {
      label: "Improvements",
      changes: [
        {
          type: "changed",
          text: "Dark mode is container-local via data-theme on the doc — two docs can render different themes on one page.",
        },
      ],
    },
    {
      label: "Fixes",
      changes: [
        {
          type: "fixed",
          text: "Code blocks keep their tokyo-night surface even when a host webview drops Shiki's CSS variables.",
        },
      ],
    },
  ]}
/>

<Release
  version="0.10"
  date="2026-05-22"
  status="done"
  title="Host-native rendering"
  tags={["Runtime", "MCP"]}
  areas={["Runtime", "MCP", "Branding"]}
  summary="The runtime renderer and theme API a companion app needs to render superlore docs in its own brand."
  media={[
    {
      type: "image",
      src: "/superlore-cloud.png",
      alt: "superlore rendered in a host app",
      caption: "superlore docs rendered live inside a host application.",
    },
  ]}
  highlights={[
    {
      icon: "code",
      title: "Render MDX anywhere",
      body: "SuperloreDoc, useSuperloreMdx and compileMdxSource render an MDX string live in any React app.",
    },
    {
      icon: "palette",
      title: "Host palette",
      body: "SuperloreTheme renders superlore in the host's brand via a CSS-variable contract.",
    },
  ]}
  sections={[
    {
      label: "New features",
      changes: [
        {
          type: "added",
          text: "superlore/runtime — render an MDX string live in any React app (SuperloreDoc, useSuperloreMdx, compileMdxSource).",
        },
        {
          type: "added",
          text: "SuperloreTheme — render superlore in the host's palette via a CSS-variable contract.",
        },
      ],
    },
    {
      label: "Improvements",
      changes: [
        {
          type: "changed",
          text: "The extension now installs from the Open VSX registry, so it auto-updates.",
          refs: [{ target: "/docs/agents", label: "Connect" }],
        },
      ],
    },
  ]}
/>

  <Release
    version="0.9"
    date="2026-04-21"
    status="done"
    title="Markdown-first authoring"
    tags={["Authoring", "Canvas"]}
    areas={["Authoring", "Canvas"]}
    summary="Write natural Markdown and get dual-representation components for free."
    sections={[
      { label: "New features", changes: [
        { type: "added", text: "Task lists render as a Checklist; NOTE/TIP alerts render as Callouts — natural markdown, dual-rep components." },
        { type: "added", text: "Canvas edge labels reserve a lane in layout, so fan-in connectors never overlap." },
      ]},
      { label: "Fixes", changes: [
        { type: "fixed", text: "Code blocks are always dark, in any page theme, on a tokyo-night surface." },
      ]},
    ]}
  />
</Releases>
