---
title: Themes
icon: Palette
description: Pick a visual skin for your KB with one flag in superlore.json. superlore ships seven themes — and your content never changes.
summary: superlore themes are opt-in visual skins set via superlore.json's theme field (default, mint, geist, ledger, obsidian, prism, paste). The package ships each skin's CSS scoped to data-sl-theme on <html>, so switching restyles the whole KB — chrome, typography, surfaces, components — with zero .mdx edits. accent and font stay orthogonal brand overrides; Canvas, dual-representation data, and the MCP are identical across every theme.
tags: [themes, design, mint, geist, ledger, obsidian, prism, paste]
---

A **theme** is an opt-in visual skin for your whole KB — chrome, typography, surfaces, and component
styling — selected with one field in `superlore.json`. Switching themes restyles everything and touches
**zero `.mdx`**. Your superpowers ride along unchanged: the Canvas stays a typed graph, every component
still serializes to its knowledge face, and the MCP serves the same corpus — **identical across every theme**.

```json
{
  "name": "your-kb",
  "theme": "geist",
  "accent": "#6D5CF0"
}
```

Under the hood, the package ships each skin's CSS scoped to `data-sl-theme` on `<html>`; the layout
stamps that attribute from your config. A theme is shipped, versioned, and inert until you opt in — not
a fork of your styles. **`accent` and `font` are orthogonal** — they layer on _any_ theme, so a preset
is never tied to a brand colour.

## The seven themes

<Table
  caption="superlore themes — pick one with theme in superlore.json"
  columns={[
    { key: "theme", label: "theme", type: "text" },
    { key: "identity", label: "Identity", type: "text" },
    { key: "feel", label: "Type & surface", type: "text" },
  ]}
  rows={[
    {
      theme: "default",
      identity:
        "superlore's native look — layered surfaces, hairline borders, violet accent, FigJam Canvas front-and-center",
      feel: "Inter + JetBrains Mono · cool white / near-black",
    },
    {
      theme: "mint",
      identity:
        "Mintlify-grade — airy blurred navbar, accent-underlined tabs, grouped icon sidebar, flat hero, release timeline",
      feel: "larger airy type · cool white / near-black",
    },
    {
      theme: "geist",
      identity:
        "Swiss border-first — structure drawn in 1px hairlines, color as punctuation, mono nav labels (inspired by Vercel)",
      feel: "sans + mono labels · pure white / true near-black",
    },
    {
      theme: "ledger",
      identity:
        "Editorial ivory — authority through restraint, a printed-essay serif body (inspired by Claude)",
      feel: "serif body + sans headings · warm cream / warm charcoal",
    },
    {
      theme: "obsidian",
      identity:
        "Dark-first dense console — near-black, information-dense, calm (inspired by Linear)",
      feel: "Inter · near-black, co-equal dim light",
    },
    {
      theme: "prism",
      identity:
        "Polished light reference — cool-white, navy headings, restrained soft elevation (inspired by Stripe)",
      feel: "airy light sans + mono · white + cool gray / deep navy",
    },
    {
      theme: "paste",
      identity:
        "Accessible system — one family, weight discipline, guaranteed contrast (inspired by Twilio Paste)",
      feel: "one super-family, 400/600 · neutral, high-contrast",
    },
  ]}
/>

<Note title="Seeing it live">
  This site runs `theme: "mint"`. Flip the flag to any name above (or remove it for `default`) and
  the same content re-renders in that skin — chrome, type, surfaces, components — with no other
  edits.
</Note>

## Brand overrides (work on any theme)

```jsonc
{
  "theme": "geist",
  "accent": "#304FFE", // one hue → the whole palette (links, active, CTA, ring), light + dark
  "font": { "sans": "Inter", "mono": "JetBrains Mono" }, // override a theme's default type
  "logo": { "light": "/logo.svg", "dark": "/logo-dark.svg", "href": "/" },
  "favicon": "/favicon.svg",
}
```

- **`accent`** is a single hue that derives the entire accent family (and the fumadocs primary/ring) for
  light and dark — no per-token overrides. A theme never hardcodes your brand colour.
- **`font`** overrides a theme's default type pairing without forking the skin.
- **`logo` / `favicon`** brand the navbar + tab the way Mintlify's `docs.json` does.

## What a theme restyles

<Table
  caption="Surfaces a theme can change"
  columns={[
    { key: "area", label: "Area", type: "text" },
    { key: "what", label: "What varies by theme", type: "text" },
  ]}
  rows={[
    {
      area: "Surface & background",
      what: "base canvas (cool-white / warm cream / near-black) + surface tints",
    },
    {
      area: "Typography",
      what: "the type pairing — sans / serif body / mono labels — and scale, weight, rhythm",
    },
    { area: "Radius & density", what: "corner rounding and reading width / vertical rhythm" },
    {
      area: "Navbar & sidebar",
      what: "blur, tab style (underline vs flat), sidebar rows + active treatment",
    },
    { area: "Hero & prose", what: "hero treatment and reading type" },
    {
      area: "Components",
      what: "cards, callouts, the release timeline — border-first vs soft elevation",
    },
  ]}
/>

The content model is **never** touched — `accent`, `font`, and the seven skins are presentation only.

## Switch themes

<Steps>
  <Step title="Set the flag">
    In `superlore.json`, set `"theme"` to any of the seven names (or omit for `default`).
  </Step>
  <Step title="That's it">
    The layout stamps `data-sl-theme` on `<html>` and the packaged skin activates. No component or
    `.mdx` changes, no CSS to write.
  </Step>
  <Step title="Keep your brand">
    `accent` drives links, the CTA, icons, and active states in every theme; `font` overrides the type.
    Set them once and the chosen theme adopts them (light and dark derived for you).
  </Step>
</Steps>

## Sidebar

Across themes, the docs sidebar collapses (hide + hover-peek — the right pattern for a nested docs tree).
Press **`⌘\`** (or `Ctrl+\`) to toggle it, and your collapsed/expanded choice **persists** across pages
and reloads.

<Tip title="Familiar on the surface, superlore underneath">
  A theme gives a team migrating from another docs tool the look their users already know — now
  backed by the Canvas engine, typed dual-representation data, and an MCP.
</Tip>
