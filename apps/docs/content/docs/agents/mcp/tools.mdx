---
title: Tools
icon: Wrench
docType: reference
description: The MCP tools superlore exposes — search, get_page, grep, glob, list, navigate, get_component_data — with their arguments and what each returns.
summary: Reference for the superlore MCP tools — search (ranked page hits), get_page (a page's body + headings + nodes), grep (regex over every page body, ripgrep-style), glob (page paths by pattern), list (filtered knowledge nodes), navigate (refs + backlinks), and get_component_data (a component's knowledge face).
tags: [mcp, agents, reference]
---

The superlore MCP server (`/api/mcp` on this deploy) exposes its corpus two ways: **read it** (the
page body and the typed knowledge nodes) and **find anything in it** (grep + glob, like a folder).
Each tool returns JSON — never scraped HTML.

<DataTable
  columns={[
    { key: "tool", label: "Tool" },
    { key: "args", label: "Arguments" },
    { key: "returns", label: "Returns" },
  ]}
  rows={[
    { tool: "search", args: "query, limit?", returns: "Ranked page hits across the corpus" },
    {
      tool: "get_page",
      args: "path",
      returns: "Frontmatter + body (MDX) + headings + ordered knowledge nodes",
    },
    {
      tool: "grep",
      args: "pattern, flags?, path?, limit?",
      returns: "Regex matches across page bodies — { path, line, text }",
    },
    {
      tool: "glob",
      args: "pattern",
      returns: "Page paths matching a shell-style glob",
    },
    {
      tool: "list",
      args: "kind?, tag?, entityType?",
      returns: "Filtered knowledge nodes across the corpus",
    },
    {
      tool: "navigate",
      args: "target",
      returns: "Outgoing refs + backlinks for a path / node id / ref",
    },
    {
      tool: "get_component_data",
      args: "id",
      returns: "A single component's knowledge face by id",
    },
  ]}
/>

## Each tool

<Steps>
  <Step title="search(query, limit?)">
    Full-text search across the documentation, ranked over the page **body** (not just frontmatter).
    Returns page hits; `title` and `summary` weigh heaviest.
  </Step>
  <Step title="get_page(path)">
    Fetch a page's full content by path (e.g. `/docs/architecture`): its frontmatter, the readable
    **body** (the raw MDX when the deploy wires a content reader, otherwise the extracted prose),
    the `headings` outline, and the ordered knowledge nodes — the serialized faces of every
    component on the page. Returns `{ error: "not_found", path }` for an unknown path.
  </Step>
  <Step title="grep(pattern, flags?, path?, limit?)">
    Regex search across every page body, line by line — ripgrep for the corpus. Returns
    `{ path, line, text }` hits. Pass `flags` (default `i`), scope with a `path` glob, and cap with
    `limit`. This is how an agent finds anything by content, the way you'd grep a folder.
  </Step>
  <Step title="glob(pattern)">
    List page paths matching a shell-style glob — `*` within a segment, `**` across segments, `?`
    for one character (e.g. everything under `/docs/agents`). The corpus's file finder.
  </Step>
  <Step title="list({ kind?, tag?, entityType? })">
    List knowledge nodes across the whole corpus, filtered by node `kind`, by frontmatter `tag`, or
    by `entityType`. Use it to enumerate, say, every `timeline` or every page tagged `roadmap`.
  </Step>
  <Step title="navigate(target)">
    Follow relations from a page path, a node id, or an entity ref. Returns the outgoing refs
    (`depends-on`, `part-of`, `related`, …) **and** the backlinks — the corpus as a graph.
  </Step>
  <Step title="get_component_data(id)">
    Get the typed data behind a single rendered component (its [knowledge
    face](/docs/authoring/dual-representation)) by id — e.g. the node/edge graph behind a Canvas,
    or the items behind a Timeline. Returns `{ error: "not_found", id }` for an unknown id.
  </Step>
</Steps>

<Note title="The same index the site renders">
  Every tool reads the structured index superlore builds from your MDX. There is no separate agent
  content to maintain — the tools and the pages are two projections of one source.
</Note>

To wire a client up to these tools, see [Connect a client](/docs/agents/mcp/connect).
