---
title: MCP
icon: Plug
docType: concept
description: Connect an agent to this knowledge base over the Model Context Protocol.
summary: superlore ships a first-class MCP server over the same structured content the site renders, mounted at /api/mcp. This overview routes to the tool reference and the connect guide.
tags: [mcp, agents, integration]
---

superlore ships a first-class **MCP server** over the _same_ structured content the site renders. On
this deploy it is **public — no auth** — and mounted at:

```
/api/mcp
```

<Note title="Same source, two faces">
  The MCP is not a scrape of these pages. It serves the structured index the site is built from, so
  what an agent reads and what a human sees can never drift.
</Note>

## Where to next

<CardGroup cols={2}>
  <Card title="Tools" icon="wrench" href="/docs/agents/mcp/tools">
    The six MCP tools, their arguments, and exactly what each returns.
  </Card>
  <Card title="Connect a client" icon="cable" href="/docs/agents/mcp/connect">
    Point Claude (or any MCP-capable client) at this deploy.
  </Card>
</CardGroup>

<Tip title="Inheriting auth">
  If a deploy gates the site (Auth.js / SSO — see [Enable auth](/docs/auth)), the MCP inherits the
  same policy. This docs deploy is intentionally open so anyone can connect.
</Tip>
